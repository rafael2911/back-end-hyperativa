spring:
  application:
    name: card-api

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

  h2:
    console:
      enabled: true
      path: /h2-console

app:
  jwt:
    secret-key: O0QA9Z9emLCVHUYVSW8yD6fFKRV4j3j+pp2MSmdxmKSnnhmcoE/NyfNEUr3MNgcWpc7HmvPvwBjZuHwZz4TPig==
    expiration-ms: 86400000

  crypto:
    secret-key: test-secret-key-for-encryption-test-only

server:
  port: 8080

logging:
  level:
    root: INFO
    com.hyperativa.api: DEBUG
    org.springframework.security: INFO

